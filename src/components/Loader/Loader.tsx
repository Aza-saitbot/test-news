import React from 'react';

const Loader = () => {
    return (
        <div style={{
            display:"grid",alignItems:"center",justifyItems:"center"
        }}>Загрузка...</div>
    );
};

export default Loader;